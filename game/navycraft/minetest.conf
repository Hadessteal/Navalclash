chat_message_format = <@name> @message
default_stack_max = 64
enable_damage = true
mg_name = singlenode

# Seconds after shift-rightclicking an active helm before the vessel converts
# back into editable world blocks. Set low while ship launch/docking is tested.
navalclash_construct_conversion_timer = 1
